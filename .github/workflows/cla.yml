name: "CLA Assistant"

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize, reopened]

jobs:
  cla:
    uses: rspace-os/.github/.github/workflows/cla.yml@main
    secrets: inherit
