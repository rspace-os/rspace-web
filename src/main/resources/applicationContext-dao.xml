<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!--  used to apply liquibase DB schema changes -->
    <bean depends-on="appContextRetriever, loggingInitializer" id="liquibase" class="liquibase.integration.spring.SpringLiquibase">
        <property name="dataSource" ref="dataSource" />
        <property name="changeLog" value="classpath:sqlUpdates/liquibase-master.xml" />
        <!-- contexts specifies the runtime contexts to use. variable is substituted 
            by Maven at build time. -->
        <property name="contexts" value="${liquibase.context}" />
    </bean>

    <bean class="org.springframework.dao.annotation.PersistenceExceptionTranslationPostProcessor" />
		
	<bean id="sessionFactory" class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
    	<property name="dataSource" ref="dataSource" />
    	<property name="configLocations" value="classpath:hibernate.cfg.xml" />
    	<property name="hibernateProperties">
    		<value>
    			hibernate.dialect=${hibernate.dialect}
    			hibernate.search.backend.type=lucene
    			hibernate.search.backend.directory.root=${rs.hibernate.searchIndex.folder}
    			hibernate.search.backend.directory.type=local-filesystem
    			hibernate.cache.use_second_level_cache=true
    			hibernate.cache.use_query_cache=true
    			hibernate.cache.region.factory_class=org.hibernate.cache.jcache.JCacheRegionFactory
    			hibernate.cache.jakarta.cache.provider=org.ehcache.jsr107.EhcacheCachingProvider
    			hibernate.cache.jcache.config_uri=classpath:ehcache.xml
			hibernate.search.indexing.plan.synchronization.strategy=sync
    			<!-- RSDEV-444 (B1): preserve the Hibernate 5 per-entity TABLE-generator layout.
    			     Hibernate 6's enhanced TableGenerator otherwise defaults bare
    			     @GeneratedValue(strategy=TABLE) entities to a single shared "default" segment row,
    			     whereas existing databases (and the two explicitly-pinned hierarchies) use one
    			     hibernate_sequences row per entity table. Setting this true makes Hibernate 6 keep
    			     using the per-entity (table-named) segment rows, matching the existing data.
    			     changeLog-rsdev-444.xml then re-seeds those rows above each table's MAX(id).
    			     NOTE: this must be confirmed on a populated DB (see the B1 verification steps in
    			     investigations/RSDEV-444-spring6-review.md); the migration also seeds a "default"
    			     insurance row so the fallback path cannot collide if this setting is ever not honoured. -->
    			hibernate.id.generator.prefer_entity_table_as_segment_value=true
    		</value>
    		<!-- Turn batching off for better error messages under PostgreSQL -->
    		<!-- hibernate.jdbc.batch_size=0 -->
    	</property>
    </bean>

</beans>
