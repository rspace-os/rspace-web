//@flow

import { type Node } from "react";
import { type HasEditableFields } from "../../stores/definitions/Editable";

export type Tag = {|
  value: string,
  vocabulary: string,
  uri: string,
  version: string,
|};

declare export default function Tags<
  Fields: {
    tags: Array<Tag>,
    ...
  },
  FieldOwner: HasEditableFields<Fields>
>({|
  fieldOwner: FieldOwner,
  loading: boolean,
|}): Node;
