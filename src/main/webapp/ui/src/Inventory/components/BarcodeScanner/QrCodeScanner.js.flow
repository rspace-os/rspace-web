//@flow

import { type Node } from "react";
import {
  type BarcodeInput,
} from "./BarcodeScannerSkeleton";

declare export default function QrCodeScanner({|
  onClose: () => void,
  onScan: (BarcodeInput) => void,
  buttonPrefix: string,
|}): Node;
