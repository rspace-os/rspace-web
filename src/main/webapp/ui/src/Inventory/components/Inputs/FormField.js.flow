// @flow

import { type Node } from "react";
import { type FormFieldArgs as BaseFormFieldArgs } from "../../../components/Inputs/FormField";

export type FormFieldArgs<T> = {|
  /**
   * When not disabled and not batch editing, all of the behaviour of the more
   * generic form field component is preserved.
   */
  ...BaseFormFieldArgs<T>,
|};

declare export default function FormField<T>(props: FormFieldArgs<T>): Node;