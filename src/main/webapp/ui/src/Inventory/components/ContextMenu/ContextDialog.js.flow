//@flow

import { type Node } from "react";

declare export default function ContextDialog({|
  children: Node,
  open: boolean,
  onClose: () => void,
  maxWidth?: "xs" | "sm" | "lg",
  fullWidth?: boolean,
|}): Node;
