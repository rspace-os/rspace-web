// @flow

import React, { type Node, type ComponentType } from "react";
import { type Record } from "../../stores/definitions/Record";

type RecordDetailsArgs = {|
  record: Record,
  hideName?: boolean,
|};

declare export default ComponentType<RecordDetailsArgs>;