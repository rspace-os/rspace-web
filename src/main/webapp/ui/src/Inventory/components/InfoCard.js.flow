// @flow

import React, {
  type Node,
  type ComponentType,
  type ElementProps,
} from "react";
import { type Record } from "../../stores/definitions/Record";

type InfoPopoverArgs = {|
  record: Record,
|};

declare export default ComponentType<InfoPopoverArgs>;