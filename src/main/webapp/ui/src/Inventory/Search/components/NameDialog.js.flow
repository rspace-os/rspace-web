// @flow

import { type Node } from "react";

declare export default function NameDialog({|
  open: boolean,
  setOpen: (boolean) => void,
  name: string,
  setName: (string) => void,
  index?: number,
  existingNames: Array<string>,
  onChange: () => void,
|}): Node;
