// @flow

import { type Node } from "react";
import {
  type SearchView,
} from "../../../stores/definitions/Search";

declare export default function ToggleView({|
  onChange: (SearchView) => Promise<void>,
  currentView: SearchView,
  views: Array<SearchView>,
|}): Node;
