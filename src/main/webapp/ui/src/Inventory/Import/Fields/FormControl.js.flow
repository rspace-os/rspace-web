// @flow

import { type Node } from "react";

type UploadFormControlArgs = {|
  label: string,
  children: Node,

  error?: boolean,
  helperText?: string,
|};

declare export default React$ComponentType<UploadFormControlArgs>;
