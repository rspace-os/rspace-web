import React from "react";
import { DataGrid } from "@mui/x-data-grid";

/**
 * A table listing all of the IGSNs that the current users owns.
 */
export default function IgsnTable(): React.ReactNode {
  return <DataGrid rows={[]} columns={[]} />;
}
