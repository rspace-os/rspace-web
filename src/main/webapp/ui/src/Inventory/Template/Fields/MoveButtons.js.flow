//@flow

import { type Node } from "react";

type MoveButtonsArgs = {|
  index: number,
  onClick: (number) => void,
|};

type CustomButtonArgs = {|
  label: string,
  onClick: () => void,
  icon: Node,
|};

declare export default function MoveButtons(args: MoveButtonsArgs): Node;