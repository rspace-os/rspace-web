// @flow

import React, { type Node, type ComponentType } from "react";
import { type Location } from "../../../../stores/definitions/Container";

type LocationWrapperArgs = {|
  children: Node,
  parentRect: {
    width: number,
    height: number,
  },
  location: Location,
|};

declare export default ComponentType<LocationWrapperArgs>;