//@flow strict

declare export function hslToHex(
  hue: number,
  saturation: number,
  lightness: number,
  opactity?: number,
): string;

declare export const selectColor: (colorNum: number, colors: number) => string;

declare export const invertColor: (inputHex: string, bw: boolean) => string;
