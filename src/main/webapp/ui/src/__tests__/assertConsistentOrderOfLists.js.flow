// @flow

export type Assertion = {
  pass: boolean,
  message?: string | (() => string),
};

export type ListName = string;

export type OrderedPairOfElementsInList = {
  list: Array<string>,
  name: ListName,
  index1: number,
  index2: number,
};

declare export function toHaveConsistentOrdering(
  mapOfListsOfNumbers: Map<ListName, Array<string>>
): any;

declare export function assertConsistentOrderOfLists(
  lists: Map<ListName, Array<string>>
): void;
