// @flow

import { type Node } from "react";
import { Order } from "./Enums";
import {
  type Cell,
} from "../../components/EnhancedTableHead";
import {
  type BookingAndEquipmentDetails,
  type BookingDetails,
  type EquipmentDetails,
  type BOOKING_TYPE,
  type EquipmentWithBookingDetails,
  makeBookingAndEquipmentData,
  makeEquipmentWithBookingData,
} from "./ClustermarketData";

type HeaderCellId = "bookingID"
| "equipmentName"
| "manufacturer"
| "model"
| "requesterName"
| "start_time"
| "duration"
| "bookingType"
| "status";

type ResultsTableArgs = {|
  clustermarket_web_url: string,
  visibleHeaderCells: Array<Cell<HeaderCellId>>,
  results: Array<BookingAndEquipmentDetails>,
  order: string,
  setOrder: ($Values<typeof Order>) => void,
  orderBy: string,
  setOrderBy: (string) => void,
  selectedBookingIds: Array<string>,
  setSelectedBookingIds: (Array<string>) => void,
  bookingType: string,
|};

declare export default function ResultsTable(ResultsTableArgs): Node;
