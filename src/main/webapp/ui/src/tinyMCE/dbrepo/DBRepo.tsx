import type { ReactNode } from "react";

/**
 * Empty DBRepo dialog body. The dialog title chrome supplied by TinyMCE
 * provides the heading.
 */
function DBRepo(): ReactNode {
  return null;
}

export default DBRepo;
