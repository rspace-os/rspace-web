//@flow

import { type Node } from "react";

declare export default function ImageEditingDialog({|
  imageFile: ?Blob,
  open: boolean,
  close: () => void,
  submitHandler: (Blob) => void,
  alt: string,
  submitButtonLabel?: string,
|}): Node;
