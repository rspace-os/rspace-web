//@flow

import { type Node } from "react";
import { type Tag } from "../../stores/definitions/Tag";

declare export default function TagListing({|
  tags: Array<Tag>,
  size?: "small" | "medium",
  endAdornment?: Node,
  showMetadataPopup?: boolean,
  onDelete?: (number, Tag) => void,
  onClick?: (Tag) => void,
|}): Node;
