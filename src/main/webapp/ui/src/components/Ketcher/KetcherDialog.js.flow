//@flow

import { type Node } from "react";
import { type ValidationResult } from "../ValidatingSubmitButton";

declare export default function KetcherDialog({|
  isOpen?: boolean,
  handleClose: () => void,
  existingChem?: string,
  title: string,
  handleInsert: (any) => void,
  actionBtnText?: string,
  readOnly?: boolean,
  additionalControls?: Node,
  validationResult?: ValidationResult;
  onChange?: () => void;
  instructionText?: string;
|}): Node;
