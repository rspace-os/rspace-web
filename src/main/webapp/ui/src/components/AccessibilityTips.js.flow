//@flow

import { type Node } from "react";

declare export function AccessibilityTipsIconButton({|
  supportsHighContrastMode?: boolean,
  supportsReducedMotion?: boolean,
  supports2xZoom?: boolean,
|}): Node;

declare export function AccessibilityTipsMenuItem({|
  supportsHighContrastMode?: boolean,
  supportsReducedMotion?: boolean,
  supports2xZoom?: boolean,
  onClose: () => void,
|}): Node;
