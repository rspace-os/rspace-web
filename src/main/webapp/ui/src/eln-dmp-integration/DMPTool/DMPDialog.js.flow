// @flow

import { type Node } from "react";

export type Plan = {
  id: number,
  title: string,
  description: string,
  modified: string,
  created: string,
};

declare export default function DMPDialog({|
  open: boolean,
  setOpen: (boolean) => void,
|}): Node;
