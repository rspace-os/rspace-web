//@flow

import { type Node } from 'react';

declare export default function Searchbox({|
  idx: number,
  query: {
    error: ?string,
    term: string,
  },
  onChange: ({ target: { value: string } }) => void,
  onSubmit: () => void,
|}): Node;
