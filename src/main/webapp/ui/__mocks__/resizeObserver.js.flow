import { vi } from "vitest";
Object.defineProperty(window, "ResizeObserver", {
  writeable: false,
  value: vi.fn<[void], { ... }>().mockImplementation(() => ({
    observe: () => {},
    disconnect: () => {},
  })),
});


