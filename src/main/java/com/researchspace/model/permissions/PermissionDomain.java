package com.researchspace.model.permissions;

public enum PermissionDomain {
	// declare in alphabetical order for sorting; enum natural sort order is its
	// declaration order
	ADMIN, ALL, GROUP, RECORD, FORM, COMMS, USER, COMMUNITY, APP
}
